#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin>>t;
    while(t--)
    {
        vector<int> amt;
        for(int i=0;i<26;i++)
        {
            int temp;
            cin>>temp;
            amt.push_back(temp);
        }
        string s;
        cin>>s;
        cout<<amt.
    }
    return 0;
}